# Minimal development requirements for running the backend locally
# This file excludes optional packages that require native compilation (e.g. aiohttp)
# Use this for local development if you cannot install MSVC/Rust toolchains.

fastapi==0.119.1
uvicorn==0.38.0
starlette==0.48.0

SQLAlchemy==2.0.44

pydantic==2.8.2

python-multipart==0.0.20

httpx==0.28.1
requests==2.31.0

python-dotenv==1.0.0
anyio==4.11.0
typing_extensions==4.14.0

# Optional extras omitted: aiohttp, aiohttp-client-cache, frozenlist, yarl, lxml, etc.